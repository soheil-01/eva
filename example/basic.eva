class Point {

    def constructor(self,x,y){
        self.x = x;
        self.y = y;
    }

    def calc(self){
        return self.x + self.y;
    }

}

let point = new Point(1,2);
point.calc(point);