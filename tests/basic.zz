class Point {

    def constructor(x,y){
        this.x = x;
        this.y = y;
    }

    def calc(){
        return this.x + this.y;
    }

}

let point = new Point(1,2);
console.log(point.calc());